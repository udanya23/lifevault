{
  "name": "lifevault",
  "version": "1.0.0",
  "private": true,
  "description": "LifeVault — Secure Emergency Information Management Platform",
  "scripts": {
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix frontend",
    "dev:all": "concurrently -n api,web -c blue,green \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
